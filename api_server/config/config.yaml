# Server configuration
server:
  timezone: "Asia/Tokyo"
  port: 8080

# VictoriaMetrics connection settings
victoria_metrics:
  url: "http://10.20.109.2:8428"

# Operation-specific settings
operations:
  lock_timeout_hours: 1
